/**
 * Creates a Set from an array.
 * @param {Array} arr - The input array of any kind of elements.
 * @returns {Set} A new Set created from the input array.
 */
function setFromArray(arr) {
  return new Set(arr);
}

export default setFromArray;
